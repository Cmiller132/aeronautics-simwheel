# registry/ — content registration

| File | Role |
|---|---|
| `SimWheelRegistry.java` | Deferred registers for the mod's first-party content: the `sim_steering_wheel` block, its item, and its block-entity type. That's the whole roster — one block is the design (DESIGN.md §5). |

Models/blockstates/lang live in
`mod/src/main/resources/assets/aeronautics_simwheel/`. The block is a
placeholder cube until the art pass.
