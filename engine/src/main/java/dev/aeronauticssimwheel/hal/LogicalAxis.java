package dev.aeronauticssimwheel.hal;

/** Logical axes v1 (DESIGN.md §5.1). BRAKE/CLUTCH reserved so bindings don't shuffle later. */
public enum LogicalAxis {
    STEERING,
    THROTTLE,
    BRAKE,
    CLUTCH
}
